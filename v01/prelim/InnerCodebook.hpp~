// A=0 T=1 G=2 C=3
class InnerCodebook{
private:
  int beta,B,nu;
  unsigned int beta4p,B2p;
  class bmatrix *CWM;  // codeword map [beta4p][nu]
  //-----
  void FuncTest();
public:
  InnerCodebook(int _beta, int _B, int _nu);  // empty
  ~InnerCodebook();
};

void InnerCodebook_PrintVect(const unsigned char *V, int len);
void InnerCodebook_ConvUintVect(unsigned int u, unsigned char *V, int len); // unsigneg int -> 4-ary vector
unsigned int InnerCodebook_ConvVectUint(  const unsigned char *V, int len); // 4-ary vector -> unsigned int   
